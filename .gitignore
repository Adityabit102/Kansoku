# Raw CWRU download (~200MB) — refetched by the pipeline; the committed
# artifacts + signal bundle keep the app fully functional without it.
data/raw/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / Next.js
node_modules/
.next/
next-env.d.ts

# OS / editor
.DS_Store
nohup.out
